CREATE TABLE user_bags (
    bag_id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    user_id INTEGER REFERENCES bugout_users(id) NOT NULL,
    bag_name TEXT NOT NULL,
    completed BOOLEAN DEFAULT FALSE NOT NULL
);