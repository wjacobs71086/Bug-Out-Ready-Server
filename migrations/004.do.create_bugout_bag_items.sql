CREATE TABLE bag_items (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    user_id INTEGER REFERENCES bugout_users(id) NOT NULL,
    bag_id INTEGER REFERENCES user_bags(bag_id) NOT NULL,
    item_id INTEGER REFERENCES bugout_items(id) NOT NULL,
    owned BOOLEAN DEFAULT FALSE NOT NULL
);